import React from "react";
import Login from "../Components/Login";
import { RegImg } from "../Components";

const SignIn = () => {
  return (
    <div className="mt-10">
      <Login />
    </div>
  );
};

export default RegImg(SignIn);
